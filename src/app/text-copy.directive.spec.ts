import { TextCopyDirective } from './text-copy.directive';

describe('TextCopyDirective', () => {
  it('should create an instance', () => {
    const directive = new TextCopyDirective();
    expect(directive).toBeTruthy();
  });
});
